{
  "name": "electron-auto-update-example",
  "version": "1.0.1",
  "description": "test for electron auto update example",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/josx/electron-auto-update-example.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --linux --publish never",
    "deploy": "electron-builder build --linux --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^8",
    "electron-builder": "^22.3.6"
  },
  "dependencies": {
    "electron-updater": "^4.3.1"
  }
}
